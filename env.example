# IdeaForge Environment Configuration

# Required: OpenAI API Key for AI analysis
OPENAI_API_KEY=your_openai_api_key_here

# Optional: n8n Webhook Configuration
# Only needed if you want external research features (Hacker News, Reddit)
# See docs/n8n-setup.md for setup instructions

# n8n base URL (default for local Docker)
N8N_BASE_URL=http://localhost:5678

# n8n webhook path
N8N_WEBHOOK_PATH=webhook

# n8n API key for authentication
N8N_API_KEY=local-dev-api-key-12345

# Request timeout in milliseconds
N8N_TIMEOUT=30000

# Number of retry attempts
N8N_RETRIES=3

# Legacy webhook URL (for backward compatibility)
N8N_WEBHOOK_URL=http://localhost:5678/webhook/ideaforge

# Optional: Reddit API Configuration
# Required for Reddit research features
# See scripts/verify-reddit-oauth.js for setup instructions

# Reddit OAuth2 Client ID (from https://www.reddit.com/prefs/apps)
REDDIT_CLIENT_ID=your_reddit_client_id_here

# Reddit OAuth2 Client Secret
REDDIT_CLIENT_SECRET=your_reddit_client_secret_here

# Reddit User Agent (format: AppName/Version (by /u/YourUsername))
REDDIT_USER_AGENT=IdeaForge/1.0 (by /u/your_username)

# Reddit API Rate Limit (requests per minute)
REDDIT_RATE_LIMIT=60

# Optional: OpenAI Model Configuration
# OPENAI_MODEL=gpt-4
# OPENAI_MAX_TOKENS=2000
# OPENAI_TEMPERATURE=0.7

# Optional: Debug Mode
# DEBUG=ideaforge:*

# Optional: Log Level
# LOG_LEVEL=info 